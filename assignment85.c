#include<stdio.h>

char check(char n);

int main()
{
 char n;
 printf("enter character:");
 scanf("%c", &n);
 check(n);
}

char check(char n)
{
 if(n==a&&n==e&&n=i&&n==o&&n==u);
 {
    printf("%c is a vowel", n);
 }
 
}